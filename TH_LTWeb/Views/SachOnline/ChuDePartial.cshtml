@model IEnumerable<TH_LTWeb.Models.CHUDE>

<ul class="list-group">
    <li class="list-group-item-active" style="font-weight: bold; text-align: center; background-color: dodgerblue; padding: 10px; ">SÁCH THEO CHỦ ĐỀ</li>
    @foreach (var item in Model)
    {
        <a href="@Url.Action("SachTheoChuDe","SachOnline", new { id = item.Name })" class="list-group-item">@item.Name</a>
    }
</ul>

